/*
function oi(nome) {
   return(`oi ${nome}`)
}

    const fun = oi('Joao')

alert(fun)
*/

function soma(x, y){
    const resultado = x + y
    return resultado;    
}

alert(soma(50, 2))
